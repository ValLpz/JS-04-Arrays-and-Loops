let usuAn= (["mockIng99", "J0eyPunch", "glassedFer"]);
    /*function userOn(usuAn) {
        if (usuAn.length === 0) {
            return "No hay usuarios en línea.";
        }
        else if (usuAn.length === 1) {
            console.log(usuAn[0] + " está en línea.");
        }
        else if(usuAn.length <2) {
            console.log(usuAn[0] + " y " + usuAn[1] +  (usuAn.length-2) + " más en línea.");
        }
    
    }//Verificar por que no arroja nada en la consola*/

console.log( usuAn[0] + " y " +usuAn[1] + " " + (usuAn.length-2) +" más en línea.");